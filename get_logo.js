require("dotenv").config(); const { PrismaClient } = require("@prisma/client"); const fs = require("fs"); const prisma = new PrismaClient(); async function main() { const school = await prisma.school.findFirst({ where: { id: 1 } }); if (school && school.logo) { if (school.logo.startsWith("data:image/png;base64,") || school.logo.startsWith("data:image/jpeg;base64,")) { const base64Data = school.logo.split(",")[1]; fs.writeFileSync("public/logo smk.png", base64Data, "base64"); console.log("Saved to public/logo smk.png!"); } else { console.log("Logo is not base64:", school.logo); } } else { console.log("No logo found in DB."); } } main().catch(console.error).finally(() => prisma.$disconnect());
