require("dotenv").config(); const { Client } = require("pg"); const fs = require("fs"); async function main() { const client = new Client({ connectionString: process.env.DATABASE_URL }); await client.connect(); const res = await client.query(`SELECT logo FROM "smk_School" WHERE id = 1`); if (res.rows.length > 0) { const logo = res.rows[0].logo; if (logo && logo.startsWith("data:image")) { const base64 = logo.split(",")[1]; fs.writeFileSync("public/logo-generic.svg", base64, "base64"); console.log("Saved to public/logo-generic.svg!"); } else { console.log("Logo is not base64"); } } else { console.log("No school row found"); } await client.end(); } main().catch(console.error);
